initSidebarItems({"fn":[["allowance","This method is used in conjunction with the approve method to query the number of approve # Example `no_run # use ontio_std::contract::{ong}; # use ontio_std::abi::{Sink, Source}; # use ontio_std::runtime::input;     let input = input();     let mut source = Source::new(&input);     let (from, to) = source.read().unwrap();     ong::allowance(from,to); `"],["approve","from-address can allow to-address to transfer a certain amount of assets from  from-address. # Example `no_run # use ontio_std::contract::{ong,TransferParam}; # use ontio_std::abi::{Sink, Source}; # use ontio_std::runtime::input;     let input = input();     let mut source = Source::new(&input);     let (from,to,amount) = source.read().unwrap();     ong::approve(from, to, amount); `"],["balance_of","Query the balance of ong assets # Example `no_run # use ontio_std::contract::{ong,TransferParam}; # use ontio_std::abi::{Sink, Source}; # use ontio_std::runtime::input;     let input = input();     let mut source = Source::new(&input);     let addr = source.read().unwrap();     ong::balance_of(addr); `"],["transfer","Transfer method of ong assets, Transfer ont assets from the from address to the to address # Example `no_run # use ontio_std::contract::ong; # use ontio_std::abi::{Sink, Source}; # use ontio_std::runtime::input;   let input = input();   let mut source = Source::new(&input);   let (from, to, amount) = source.read().unwrap();   ong::transfer(from,to, amount); `"],["transfer_from","Spender transfers a certain amount of ong from from-address to to-address # Example `no_run # use ontio_std::contract::{ong,TransferParam}; # use ontio_std::abi::{Sink, Source}; # use ontio_std::runtime::input;     let input = input();     let mut source = Source::new(&input);     let (spender, from, to, amount) = source.read().unwrap();     ong::transfer_from(spender, from, to, amount); `"],["transfer_multi","transfer_multi method of ong assets,Multiple transfers in one transaction # Example `no_run # use ontio_std::contract::{ong,TransferParam}; # use ontio_std::abi::{Sink, Source}; # use ontio_std::runtime::input; # use ontio_std::types::{Address,U128};     let input = input();     let mut source = Source::new(&input);     let trs: Vec<(Address,Address,U128)> = source.read().unwrap();     let mut transfers = Vec::<TransferParam>::new();     for tr in trs.iter() {         transfers.push(TransferParam{             from:tr.0,             to:tr.1,             amount:tr.2,         })     }     ong::transfer_multi(transfers.as_slice()); `"]]});